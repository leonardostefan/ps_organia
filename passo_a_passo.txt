### TODO: Remover esse arquivo de rascunho

# Build docker

rodar 
```shell
./docker_build.sh
```

Criar database Organia (manual) 
cria tabelas 


```sql
CREATE TABLE reviews (
    id SERIAL PRIMARY KEY,
    text TEXT NOT NULL,
    costumer_name TEXT NOT NULL,
    sentiment VARCHAR(20) NOT NULL,
    created_at TIMESTAMP NOT NULL
);
```



# Rodando o app:
devcontainer:

```shell
uvicorn main:app --host 0.0.0.0
```